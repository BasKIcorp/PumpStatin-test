# Copy to production.env on server
NODE_ENV=production
PORT=5000
USE_SQLITE=true
SQLITE_PATH=/opt/pumpstatin-test/data/app.sqlite
# Optional external backend (if set, overrides SQLite for /api)
# BACKEND_API_URL=http://127.0.0.1:8000
